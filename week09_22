<!DOCTYPE html>
<html>
    <head>
        <title>실습22번문제</title>
    </head>
    <body>
        <script type="text/javascript">
            var num1 = prompt("숫자를 입력하세요.");
            var math = prompt("연산 기호를 입력하세요.");
            var num2 = prompt("숫자를 입력하세요.");

            if (math == "+")
            {
                result = num1 + num2;
            }
            else if (math == "-")
            {
                result = num1 - num2;
            }
            else if (math == "*")
            {
                result = num1 * num2;
            }
            else if (math == "%")
            {
                result = num1 % num2;
            }
            document.write(result);
        </script>
    </body>
</html>
